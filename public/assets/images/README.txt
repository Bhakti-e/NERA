Place the official NERA logo here:
  nera-logo.png

The logo component at /components/brand/NERALogo.tsx
will load it automatically at /assets/images/nera-logo.png.

If the file is missing, the site shows a clean SVG fallback — layout is never broken.

Logo rules:
- Has a white background — only use on white/light surfaces
- For dark backgrounds, use the <NERALogoOnDark /> component variant
- Do not crop, recolor, or recreate the logo
